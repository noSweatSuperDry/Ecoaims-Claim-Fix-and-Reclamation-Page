import React from "react";

function LogOut() {
  const handleSubmit = () => {};

  return (
    <div>
      <button className="idPassCard" onClick={handleSubmit}>
        Log Out
      </button>
    </div>
  );
}

export default LogOut;
